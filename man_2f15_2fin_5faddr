#title struct in_addr

=== struct in_addr ===
인터넷 주소를 저장하기 위해서 사용한다.
 * /usr/include/netinet/in.h에 정의되어 있다.
   {{{#!plain
typedef uint32_t in_addr_t;
struct in_addr
{
    in_addr_t s_addr;
};
}}}
 * uint32_t는 unsigned int의 typedef 자료형이다.
 * inet_aton(3)

