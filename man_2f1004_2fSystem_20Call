#title 시스템 콜, 시스템 호출

(!) '''시스템 콜''', '''시스템 호출'''

This document explains a way to add a system_call(:12) to linux(:12) kernel(:12)
 1. 이 문서는 리눅스 커널에 시스템 호출을 추가하는 방법에 대한 내용을 담고 있다.
 1. 이 문서는 리눅스 커널에 시스템 호출함수를 추가하는 방법을 설명하고 있다.

<!> 문맥에 따라서, 시스템 호출 함수로 번역할 수도 있다.
