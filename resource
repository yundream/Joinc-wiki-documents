#title 시스템 자원 관련 
[wiki:function_index 함수분류로 이동]
----
 * getrlimit(2)
   자원의 제한값을 얻어온다.  
 * setrlimit(2)
   자원의 제한값을 설정한다.
 * sysinfo(2) 
   시스템의 정보 얻어오기
 * ttyname(2)  
   터미널의 이름을 얻어온다.
 * getenv(3)
   환경변수 값 얻어오기
 * putenv(3)
   환경변수 설정
 * isatty(3)
   터미널인지 검사 
 * setenv(3)
   환경변수 설정
